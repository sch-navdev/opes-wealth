[[PROJECT_TRACKER|← Project Tracker]]

# Portfolio Dashboard

**Status:** Done (foundation) — Phase 1, Step 7. Extended with edit/delete and multi-currency display.

## Foundation
- `src/app/dashboard/actions.ts` — server action `addAsset(formData)`: reads `name`, `category_id`, `quantity`, `current_value`, `currency` (default `'USD'`) from the form, resolves the current user via `getUser()`, inserts into `assets` with `profile_id` set to that user's id, and calls `revalidatePath('/dashboard')` on success.
- `src/components/add-asset-dialog.tsx` — Client Component using shadcn `Dialog`/`Select`/`Input`/`Label`. Submits `addAsset` via a client-driven `startTransition` (not a bare form `action`) so it can show inline errors and close the dialog only on success. Category dropdown is populated from the `asset_categories` passed in as a prop.
- `src/app/dashboard/page.tsx` — now also queries `asset_categories` (all rows, for the dropdown) and the signed-in user's own `assets` (joined with `asset_categories(name)`), in parallel. Renders `AddAssetDialog` at the top of the page and a shadcn `Table` of the user's assets below it (Name / Category / Quantity / Value, liabilities shown in destructive red with a leading `-`). Empty state shown when there are no assets yet.
- Installed shadcn `dialog`, `select`, `table` components (`src/components/ui/`); fixed the same CLI `cn`-package import issue as previous component installs.
- **Resolved**: the missing-`profiles`-row gap that would have made `addAsset` fail with a foreign-key violation is now fixed by `supabase/migrations/0002_user_profile_trigger.sql` (see [[Database-Schema|Database Schema]]).
- Not yet verified against a live Supabase instance (dev server was only exercised while signed out, since these routes are visible only to newly-verified sessions and no test account/schema deployment was available in this session).

## Edit/Delete Asset Actions
`src/app/dashboard/actions.ts` gained `updateAsset(id, formData)` (same field extraction as `addAsset`, `.update(...).eq('id', id).eq('profile_id', user.id)`, `revalidatePath('/dashboard', 'layout')`) and `deleteAsset(id)` (same ownership-scoped `.delete()`). Both re-check `profile_id = user.id` explicitly in addition to relying on RLS, so a row that isn't the caller's own is a no-op rather than a surprise. `add-asset-dialog.tsx` now accepts an optional `asset` prop; when present it's in edit mode — every field is pre-filled from that asset (including [[Real-Estate-Multi-Currency|Real Estate]] metadata), the trigger becomes an `Edit`-icon-only button instead of "Add Asset", the title/description/submit-button text switch to "Edit Asset"/"Save Changes", and submission calls `updateAsset(asset.id, formData)` instead of `addAsset`. Installed shadcn `alert-dialog` (same `cn`-import fix as before); `src/components/delete-asset-button.tsx` wraps it — clicking the `Trash2`-icon button opens a double-confirmation dialog ("Are you sure you want to delete this asset? This action cannot be undone."), and `AlertDialogAction`'s `onClick` calls `e.preventDefault()` before running `deleteAsset` so the dialog stays open while the delete is pending and only closes on success (Radix's `Action` would otherwise close it immediately on click). `dashboard/page.tsx`'s table gained a right-aligned "Actions" column rendering both per row.

Verified visually in the browser (via a disposable local-only preview route, removed before committing): editing a sample Real Estate asset correctly pre-fills every field and the button reads "Save Changes"; the delete button opens the exact confirmation copy requested.

## Asset Image / Logo
- `supabase/migrations/0005_asset_image.sql` adds `assets.image_base64` (`text`, nullable) — see [[Database-Schema|Database Schema]].
- `src/lib/crop-image.ts` gained `resizeImageToBase64(file, maxSize = 400, quality = 0.8)`: reads the file, downscales it (preserving aspect ratio, no crop step) to fit within 400×400, and re-encodes as a JPEG data URL — a simpler sibling of `getCroppedImage` (used by the circular profile-avatar cropper in [[Profile-Settings|Profile & Settings]], which does crop).
- `add-asset-dialog.tsx` — new "Image / Logo" field at the top of the form: a rounded-square `Avatar` preview (falls back to the selected category's first letter), an "Upload Image"/"Change Image" button, and a remove (×) button once an image is set. Selecting a file resizes it via `resizeImageToBase64` into state, mirrored into a hidden `image_base64` form input. In edit mode, `AssetForEdit` carries `image_base64` so the preview pre-fills from the existing asset; `resetState()` reverts it on cancel like every other field.
- `src/app/dashboard/actions.ts` — `addAsset`/`updateAsset` now also read `image_base64` from the form and write it straight through to the `assets.image_base64` column (same opaque-passthrough pattern as `metadata`).
- `dashboard/page.tsx` — the Name column now shows a small rounded-square `Avatar` to the left of the name: the asset's `image_base64` if set, otherwise a `Building2` icon for Real Estate or the category's first letter for anything else.
- Verified interactively via a disposable preview route: uploading a test image correctly resized/previewed it and swapped the button to "Change Image" with a remove control; clicking remove reverted to the placeholder and "Upload Image". Not yet verified against a live Supabase instance.

## Asset Details Page & Clickable Rows
- `src/app/dashboard/assets/[id]/page.tsx` — new Server Component route. Fetches the single asset by `id` scoped to `profile_id = user.id` (same RLS-plus-explicit-filter pattern as `updateAsset`/`deleteAsset`); calls `notFound()` if it doesn't exist or isn't the caller's own. Same auth + AAL2 guard as every other `/dashboard/*` page. Renders a header Card (avatar, name, category, Off-Plan badge, Net Equity/Value), then for Real Estate assets: a Characteristics Card (address, type, surface area, year, EPC, condensed condition ratings), a Financials Card (total market valuation, ownership list), and — only when `metadata.is_offplan` — an Off-Plan Payment Tracking Card with Contract Price/ADM Fee/Paid-to-Date/Outstanding Balance plus a shadcn `Table` of the payment schedule (each milestone's status shown as a green "Paid" or muted "Pending" `Badge`).
- `dashboard/page.tsx` — each asset `TableRow` gained `cursor-pointer hover:bg-muted/50`; the Name cell's contents (avatar, name, Off-Plan badge) are now wrapped in a `<Link href={\`/dashboard/assets/${asset.id}\`}>` rather than plain text, since the page is a Server Component and can't carry a row-level `onClick` (event handlers can only live inside the Client Components already rendered in that row).
- **Stray-click guard**: since the Edit and Delete controls live in a separate table cell from the Name-column `Link` (not nested inside it), a click on them was never going to bubble into that link — but per the task's explicit ask, the Edit trigger (`add-asset-dialog.tsx`) and the Delete trigger/Cancel/Confirm buttons (`delete-asset-button.tsx`) now all call `e.stopPropagation()` in their `onClick` handlers anyway, as defensive insurance against a future row-level click handler being added.
- Verified visually via a disposable preview route (a standalone copy of the details page's JSX with mock off-plan data, since the real route needs a live Supabase session): header, Characteristics, Financials, and the Off-Plan card with its two-row payment-schedule table (one "Paid", one "Pending") all rendered correctly.
- Not yet verified against a live Supabase instance — in particular, clicking through from a real table row, and `notFound()` behavior for a missing/foreign asset id, haven't been exercised end-to-end.

## Performance Column & Badge Truncation Fix
- **`src/components/portfolio-table.tsx`** (not `dashboard/page.tsx` — the table was extracted into this Client Component during [[Privacy-Mode|Privacy Mode]] so it could call `usePrivacy()`) gained a "Performance" column between Value and Actions. For Real Estate rows: `calculateTotalCost`/`calculateUnrealizedGain` (from [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]]) compute the gain against market valuation, `convertAmount` converts it into the display currency, and the cell renders a `flex flex-col` with the signed amount on top and the signed percentage below, both colored `text-emerald-500`/`text-red-500`/`text-muted-foreground` by sign and passed through `maskValue()`. Non-Real-Estate rows (no cost-basis fields exist for them) show "—".
- **`asset-detail-view.tsx`**'s Aperçu → Unrealized Gain card: the row wrapping the value and the percentage `Badge` is now `flex w-full flex-wrap items-center gap-2` (was missing `flex-wrap`/`w-full`), and the badge itself gained `whitespace-nowrap px-2 py-0.5` so a value like "+16.0%" can't get clipped by the card's fixed width — it now wraps to its own line on narrow viewports instead of being cut off mid-digit.
- Verified visually via a disposable preview route: the Performance column showed `+AED 106,000.00` / `+9.27%` in green for a gaining asset, `-AED 244,000.00` / `-21.33%` in red for a losing one (hand-checked against `calculateTotalCost`/`calculateUnrealizedGain`'s own arithmetic), and "—" for a Cash asset; the Unrealized Gain badge rendered fully as "+9.3%" at both desktop width and a 340px mobile width, with no truncation at either.
- Not yet verified against a live Supabase instance.

## Dashboard Metric Cards
- **Context**: same 21st.dev MCP unavailability as [[Authentication-Security|the login redesign]] — hand-built with shadcn/ui instead of fetching a metric-card/table component.
- `src/components/dashboard-metric-cards.tsx` (new, Client Component) — a 4-card `grid` above the Portfolio table on `dashboard/page.tsx`: **Net Worth**, **Total Assets** (sum of non-liability rows), **Total Liabilities** (sum of liability rows, `text-destructive` when non-zero), and **Real Estate Unrealized Gain** (sum of `calculateUnrealizedGain` across every Real Estate asset — the same helper the [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]] cost-basis cards already use — colored success/destructive by sign, with an `ArrowUpRight`/`ArrowDownRight` icon). Every figure is run through `usePrivacy()`'s `maskValue()`, same as every other financial figure in the app — confirmed by reading the component: all four `value` props are wrapped in `maskValue(...)` before render, no unmasked path exists.
- `dashboard/page.tsx` — computes `totalAssetsValue`/`totalLiabilitiesValue` (simple currency-converted sums, filtered by `is_liability`) and `totalUnrealizedGain` (loops every asset, skips non-Real-Estate, reuses `parseRealEstateMetadata`/`calculateTotalCost`/`calculateUnrealizedGain`) alongside the pre-existing `totalNetWorth` calculation, then passes all four (pre-formatted with the page's existing `currencyFormatter`) to the new component.
- Card labels are wired through the [[Localization|Localization]] system (`t("total_assets")`, `t("total_liabilities")`, `t("real_estate_unrealized_gain")`, plus the existing `net_worth` key) for consistency with the rest of the translated dashboard chrome.
- Verified: `tsc --noEmit` clean. **Not verified against real portfolio data** — couldn't log into a test account in this environment (no credentials/session available), so the Manarat Living III off-plan asset the task asked to test against was never actually exercised through this UI. Verified only by code review: the masking wrapper is present on every value, and the aggregation logic reuses the exact same, already-shipped helpers the Performance column and asset details page use (not new arithmetic).

## Category-Specific Settings Views (Vehicles, Private Equity)
- `asset-detail-view.tsx`'s Settings/Paramètres tab now renders a category-specific detail card, the same conditional-block pattern the Real Estate cards already use (`isRealEstate && (...)`), extended with `isVehicle`/`isPrivateEquity` derived from `categoryName`: a **Vehicle Details** card (Make, Model, Year, VIN, via `parseVehicleMetadata`) and a **Private Equity Details** card (Entity Name, Share Class, Ownership Percentage — masked through `maskValue()` like every other ownership figure in the app, via `parsePrivateEquityMetadata`). Both use only semantic theme tokens (`border-border`, `bg-card`, `text-foreground`, `text-muted-foreground`), consistent with the rest of the file.
- Only the Settings tab is category-aware for these two — the Overview tab's Real-Estate-only metric cards (Total Property Cost, Unrealized Gain, etc.) still render for every category showing "—", same pre-existing behavior as any non-Real-Estate asset (Cash, Equities, …); not a regression introduced here, and out of scope for what was asked.
- **Mock-auth bypass extended to `/dashboard/assets/[id]/page.tsx`** (previously only `/dashboard/page.tsx` had it) — same `NODE_ENV === "development"`-gated pattern, needed so this new UI could actually be verified end-to-end without a real login session. See [[Authentication-Security|Authentication & Security]].
- Verified in the browser against the two live Vehicles/Private Equity rows (see [[Database-Schema|Database Schema]]): both detail pages load without crashing, correct header (name/category/value/avatar initial), and the new Settings cards show the right fields — confirmed in both dark and light mode (light-mode screenshot-checked for the Private Equity card specifically).

## Vehicles & Private Equity Add/Edit Forms
- **Why**: the Add/Edit dialog (`add-asset-dialog.tsx`) only ever rendered `RealEstateFields` for a category-specific sub-form — Vehicles and Private Equity assets could only be created/edited with the generic Name/Category/Quantity/Value/Currency fields, even though [[#Category-Specific Settings Views (Vehicles, Private Equity)|the read-only Settings tab]] already displayed their metadata.
- `src/components/vehicle-fields.tsx` (new) — Make/Model/Year/VIN inputs, mirroring `RealEstateFields`'s layout conventions (`w-full min-w-0`, `border-t border-border pt-6` section divider) but using `useLanguage()`'s `t()` for every label/placeholder instead of hardcoded English, since this is new work rather than a retrofit of the existing (still-untranslated, see [[Localization|Localization]]) dialog chrome.
- `src/components/private-equity-fields.tsx` (new) — Entity Name/Share Class/Ownership Percentage (a `%`-suffixed number input, `min="0" max="100"`), same pattern.
- `add-asset-dialog.tsx` — added `vehicleMetadata`/`privateEquityMetadata` state (parsed via `parseVehicleMetadata`/`parsePrivateEquityMetadata` in edit mode, reset alongside every other field on cancel/close) and `isVehicle`/`isPrivateEquity` category checks alongside the existing `isRealEstate` one. Both new field blocks render conditionally, same as `RealEstateFields`.
- **Validation**: `lib/vehicles.ts`/`lib/private-equity.ts` gained `getVehicleMetadataErrors()`/`getPrivateEquityMetadataErrors()` — the same "collect every unmet requirement, return translation keys not raw strings" pattern as `getPasswordRequirementErrors` in `auth-validation.ts`. `handleSubmit` runs the relevant validator before building the `metadata` JSON payload; on any error it calls `t(errors[0])` and blocks submission (`return` before `startTransition`) instead of sending a malformed/incomplete payload to `addAsset`/`updateAsset`. The Ownership Percentage field's native `min`/`max` also blocks out-of-range values via the browser's own constraint validation before the custom validator even runs — verified in-browser that entering `150` shows the native "Value must be less than or equal to 100." tooltip and never reaches the submit handler.
- No `Json`/TypeScript friction: `VehicleMetadata`/`PrivateEquityMetadata` are plain `{string | number | null}` objects, structurally compatible with the generated `Json` type `assets.metadata` is typed as — `tsc --noEmit` stayed clean before and after.
- **Display**: `portfolio-table.tsx`'s avatar-fallback icon (previously `Building2` for Real Estate, else a bare initial) now also shows `Car` for Vehicles and `Briefcase` for Private Equity.
- **Verified in the browser** (dev server + the existing mock-auth bypass, against the two real Vehicles/Private Equity rows — see [[Database-Schema|Database Schema]]): editing the Porsche row correctly pre-filled Make/Model/Year/VIN; clearing VIN and submitting showed "VIN is required." and blocked the save (no network call); editing the Navtec Group row and clearing Entity Name showed "Le nom de l'entité est requis." in French after toggling the language switcher — confirming the new components pick up `useLanguage()` correctly. A real save couldn't be exercised end-to-end: `updateAsset`'s own `getUser()` check has no mock-auth bypass (only the page-load SSR read does — see [[Authentication-Security|Authentication & Security]]), so submitting valid data correctly reached the server action but failed there with "You must be signed in" — an environment limitation, not a defect in this feature.

## Related
- [[Database-Schema|Database Schema]] — `assets` table, `profiles` trigger
- [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]] — Real Estate sub-form rendered inside the Add/Edit dialog, FX conversion of the Value column, the cost-basis helpers the Performance column reuses
- [[Design-System|Design System]] — theme tokens used by the table/dialog
- [[Profile-Settings|Profile & Settings]] — shares the resize/compress-to-Base64 image pipeline with the profile avatar uploader
- [[Privacy-Mode|Privacy Mode]] — `portfolio-table.tsx`'s extraction into a Client Component, and the `maskValue()` calls in the new Performance column
