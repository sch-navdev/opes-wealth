[[PROJECT_TRACKER|← Project Tracker]]

# Changelog

Flat, chronological log. One line per unit of work — details live in the relevant module note (see [[PROJECT_TRACKER|Project Tracker]] → Modules).

- 2026-09-17: Project scaffolded with Next.js (TypeScript, Tailwind, ESLint, App Router, `src/` dir); shadcn/ui initialized (New York, Zinc); Supabase client libraries installed; Git repo initialized and pushed to GitHub.
- 2026-09-17: PROJECT_TRACKER.md created.
- 2026-09-17: Implemented Opes Wealth luxury design system (Midnight Navy & Champagne Gold, dark-mode-first, 0px border radii) in `globals.css`. See [[Design-System|Design System]].
- 2026-09-17: Configured Supabase keys (`.env.local`) and implemented Supabase browser/server clients plus auth session-refresh middleware (`src/middleware.ts`). Build verified with zero TS/bundling errors. See [[Authentication-Security|Authentication & Security]].
- 2026-09-17: Generated initial wealth-tracking schema migration (`profiles`, `asset_categories`, `assets`, `asset_history`) with Row Level Security policies in `supabase/migrations/0001_initial_schema.sql`. Syntax verified with the real Postgres grammar (`libpg-query`); not yet applied to any database. See [[Database-Schema|Database Schema]].
- 2026-09-18: Implemented authentication UI and protected dashboard: login/signup/logout server actions, `/login` page (shadcn Card/Input/Label/Button, dark luxury theme), and `/dashboard` as a protected Server Component redirecting unauthenticated users to `/login`. Verified visually in the browser. Build verified with zero TS/bundling errors. See [[Authentication-Security|Authentication & Security]].
- 2026-09-18: Implemented TOTP 2FA: login-time AAL2 check and redirect to `/login/mfa`, `verifyMfaLogin` server action, `/login/mfa` verification page, `/dashboard/mfa` enrollment page (QR code + activation), and AAL2 enforcement on `/dashboard`. Verified visually. Build verified with zero TS/bundling errors. See [[Authentication-Security|Authentication & Security]].
- 2026-09-18: Implemented Passkeys (sign-in, MFA enrollment, MFA login verification) and Remember Me (session-only vs. persistent auth cookie). Substituted the SDK's real passkey APIs for the non-existent `signInWithWebAuthn()` named in the request. Verified visually. Build verified with zero TS/bundling errors. See [[Authentication-Security|Authentication & Security]].
- 2026-09-18: Switched passkey registration from `mfa.webauthn.register()` to `supabase.auth.registerPasskey()` — the project's Supabase instance returns "MFA enroll is disabled for WebAuthn" for the MFA-factor route. Added try/catch around `signInWithPasskey()` for a cleaner failure message. Build verified with zero TS errors. See [[Authentication-Security|Authentication & Security]].
- 2026-09-18: Added `needsMfaStepUp()` helper so a Passkey-authenticated session bypasses `/login/mfa` entirely. Added a "← Back to Dashboard" link and an "Active authenticators" list to the MFA setup page. Built the portfolio dashboard foundation: `addAsset` server action, `AddAssetDialog`, and a shadcn Table of assets on `/dashboard`. Flagged a real gap: `addAsset` would fail until signup creates a matching `profiles` row. Build verified with zero TS/bundling errors. See [[Authentication-Security|Authentication & Security]], [[Portfolio-Dashboard|Portfolio Dashboard]].
- 2026-09-18: Added `supabase/migrations/0002_user_profile_trigger.sql` — auto-creates a `profiles` row for every new `auth.users` row, fixing the foreign-key gap noted above. Removed the dead "Verify with Passkey" button/logic from `mfa-form.tsx`. Build verified with zero TS/bundling errors. See [[Database-Schema|Database Schema]].
- 2026-09-18: Refined `needsMfaStepUp()` to check `currentAuthenticationMethods` for a `passkey`/`webauthn` entry and bypass the MFA step immediately if found. Build verified with zero TS errors. See [[Authentication-Security|Authentication & Security]].
- 2026-09-18: Built Profile Settings: `0003_profile_extended_fields.sql`, `dashboard/settings/actions.ts`, `dashboard/settings/page.tsx`, and `profile-form.tsx` (email verification badge/CTA, Base64 avatar upload, name/phone/address fields, phone-verification stub). Updated `/dashboard`'s header to show the user's avatar, first name, and a "Profile Settings" link. Build verified with zero TS/bundling errors. See [[Profile-Settings|Profile & Settings]], [[Database-Schema|Database Schema]].
- 2026-09-18: Raised the Server Action body size limit to `5mb` for avatar uploads. Added a `react-easy-crop`-based circular avatar cropper. Added `countries.ts` (~195 countries) and a reusable searchable country/phone combobox. Verified end-to-end via a disposable preview route. Build verified with zero TS/bundling errors. See [[Profile-Settings|Profile & Settings]].
- 2026-09-21: Ran a full codebase review (three parallel audits) before making any changes, per explicit request. Rebuilt the still-boilerplate root page and rebranded layout metadata; fixed radius-token and destructive-color drift; baked the champagne-gold outline treatment into `Button`; fixed a non-null assertion, a stray `alert()`, a missing label association, an untyped branch in `mfa.ts`, and added mobile grid fallbacks; split the MFA setup page's merged list into two sections; removed the unused `form.tsx` component and its dependencies. Build verified with zero TS/bundling errors. See [[Codebase-Audits|Codebase Audits]].
- 2026-09-21: Moved 2FA management from `/dashboard` to `/dashboard/settings`. Refactored `profile-form.tsx` with an edit-state toggle (disabled by default, "Edit Profile" → "Save Changes"/"Cancel"). Added an editable "Email Address" field and `updateEmail()` server action. Added a temporary debug log to `mfa.ts` to capture the real AMR string from a device passkey login (flagged for removal). Build verified with zero TS/bundling errors. See [[Profile-Settings|Profile & Settings]], [[Authentication-Security|Authentication & Security]].
- 2026-09-21: Removed the temporary debug log from `mfa.ts` and updated `needsMfaStepUp()` to also treat `"passkey"` as an instant, permanent MFA bypass. Added `0004_real_estate_and_currency.sql` (`assets.metadata` jsonb). Built the expanded Real Estate asset form with a dynamic ownership list. Added a Currency selector and a global `CurrencySwitcher` + live FX conversion to `/dashboard`, fixing a latent bug where the Value column was always formatted as USD. Build verified with zero TS/bundling errors. See [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]], [[Database-Schema|Database Schema]].
- 2026-09-21: Switched `fx.ts` from Frankfurter to `open.er-api.com` (Frankfurter doesn't support AED) and lengthened its cache from 1h to 12h. Added `updateAsset`/`deleteAsset` server actions and `parseRealEstateMetadata()`. `add-asset-dialog.tsx` now doubles as an edit form via an optional `asset` prop. Added a right-aligned "Actions" column (Edit/Delete) to the portfolio table. Build verified with zero TS/bundling errors. See [[Portfolio-Dashboard|Portfolio Dashboard]], [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]].
- 2026-09-21: Added off-plan property tracking: Contract Price, ADM-fee percent/amount pair, and a Payment Schedule milestone builder, with Total Paid to Date and Outstanding Balance computed automatically. `add-asset-dialog.tsx` computes Net Equity (market valuation minus outstanding balance) as the stored `current_value` for off-plan assets. Portfolio table shows an "Off-Plan" badge and a Total/Owed subtitle. Build verified with zero TS/bundling errors. See [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]].
- 2026-09-21: Added `0005_asset_image.sql` (`assets.image_base64`) and a per-asset image/logo uploader in `add-asset-dialog.tsx` (resize-to-400×400 via new `resizeImageToBase64()`, preview + remove control, pre-fills in edit mode). Portfolio table now shows a small avatar next to each asset's name (the image, or a `Building2` icon/category initial as fallback). Verified interactively via a disposable preview route. Build verified with zero TS/bundling errors. See [[Portfolio-Dashboard|Portfolio Dashboard]], [[Database-Schema|Database Schema]].
- 2026-09-21: Added a dedicated asset details page (`/dashboard/assets/[id]`, Server Component, `notFound()` on a missing/foreign id) with header/Characteristics/Financials/Off-Plan-Payment-Tracking Cards, the last showing the full payment-schedule table with Paid/Pending badges. Portfolio table rows are now `cursor-pointer hover:bg-muted/50` with the Name cell wrapped in a `Link` to that page; Edit/Delete/Cancel/Confirm buttons all call `e.stopPropagation()` as a defensive guard. Verified visually via a disposable preview route. Build verified with zero TS/bundling errors. See [[Portfolio-Dashboard|Portfolio Dashboard]].
- 2026-09-21: Replaced `assets.image_base64` with `assets.images` (multi-image, capped at 3) and added a Linked Loan (amount/rate/duration/start date) to Real Estate metadata, now subtracted from Net Equity for every Real Estate asset. Added `updateAssetValuation()` writing to `asset_history` (extended with `net_equity`/`source`). Refactored the asset details page into a Server-fetch + Client-Tabs split (`asset-detail-view.tsx`) with Aperçu (recharts history graph + key metrics), Analyse (Market Performance, Gross/Net ownership table, Characteristics, Off-Plan tracking), and Paramètres (Edit/Delete) tabs, plus a clickable-avatar image carousel and a Refresh Valuation dialog. Build verified with zero TS/bundling errors. See [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]], [[Database-Schema|Database Schema]].
- 2026-09-21: Added `calculateTotalCost`/`calculateCashInvestedToDate`/`calculateUnrealizedGain` (all-in cost basis including the ADM fee, not just purchase price + fees) and replaced the Aperçu tab's single "Invested Capital" card with three cards (Total Property Cost, Unrealized Gain with a %-badge, Cash Invested to Date). Added a currency selector + FX conversion to the Refresh Valuation dialog, and currency-symbol prefixes on every price/fee input across the Add/Edit form and Refresh Valuation dialog. Build verified with zero TS/bundling errors. See [[Real-Estate-Multi-Currency|Real Estate & Multi-Currency]].
